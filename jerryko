📚 학습 주제
주차: [주차번호] 4-2
주제: [학습 주제명] 객체
📝 학습 내용 체크리스트
✅ 개념 요약
1.정의

여러 데이터를 하나의 변수에 묶어서 관리할 수 있는 구조
키(key)와 값(value) 쌍으로 이루어짐
2.만들기 -> let obj = {};

3.읽기/쓰기/삭제

const user = {};
쓰기: user.name = "Tom";
읽기: console.log(user.name);
삭제: delete user.name;
4.점vs대괄호

점: user.name = "Ann";
대괄호: user["likes birds"] = true; (공백·한글·특문 가능)
변수 키 지정: const k = "age"; user[k] = 15; 
5.프로퍼티 접근

점 표기법: obj.key
키가 유효 식별자(공백 없음, 숫자 시작 불가, 특수문자 제외(_, $는 허용))일 때만 사용 가능
대괄호 표기법: obj["key"]
임의의 문자열·동적 표현식 사용 가능
💻 예시 코드 및 설명
 개념을 설명하는 실용적인 코드 예시를 작성했는가?
 코드에 대한 상세한 설명을 포함했는가?
📋 주요 학습 개념 설명/요약 (선택)
배열은 값만 저장하는데, 객체는 의미 있는 이름(key)으로 데이터를 저장할 수 있다는 게 차이점
자바스크립트에서 거의 모든 게 객체 (배열, 함수도 사실 객체의 한 종류)
실무에서 데이터는 대부분 객체 형태(JSON)로 주고받음
❓ 질문사항 및 토론 포인트
[궁금한 점이나 토론하고 싶은 내용]
점 표기법과 대괄호 표기법은 어떤 기준으로 선택하나요? 큰 특징을 이해하면 구분하기 좋을 것 같은데, 글을 읽어도 이해가 잘 안되서 질문드립니다.
객체 복사할 때 JSON을 쓰는 걸 보았는데, 정확히 언제 써야 되는지 궁금합니다. (데이터 주고 받을 때만 쓰는 것인지..(?)) 그리고 JSON이랑 일반 객체가 어떻게 다른지도 함께 논의해 보고 싶습니다.
